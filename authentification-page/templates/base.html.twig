<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
        <title>{% block title %}{% endblock %}</title>
    </head>
    <body>
        <div class="container">

            <nav class="navbar navbar-light bg-light">
                <div>
                    <h2><a href="{{ path('home_main') }}">Test Symfony</a></h2>
                </div>
                
                <div id="right">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a href="{{ path('app_logout') }}" class="btn btn-secondary">Déconnexion</a>
                    {% else %}
                    <span class="mr-2">Bienvenue invité</span>
                    <a href="{{ path('user_add') }}" class="btn btn-success">Inscription</a>
                    <a href="{{ path('app_login') }}" class="btn btn-primary">Connexion</a>
                    {% endif %}

                </div>
            </nav>
            
            <hr>

            {# display every flash message available #}
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}

            <div class="row">
                <div class="col">
                    {% block body %}{% endblock %}
                </div>
            </div>

            <footer>
                <hr>
                <p>Test en Symfony</p>
            </footer>
            
        </div>
    </body>
</html>